# 维护

- 应用：开放平台「甲骨易财务连接器」Client ID `357164`。**不要点重置密钥。**
- 总部服务 `795589109148` / 账套 `1783803326505631821`。禁止解绑。
- 本机连接器：`~/.config/finance/kingdee.local.json`（client）+ `kingdee-pl.local.json`（多账套表）
- 本机网页登录：`~/.config/finance/xingchen.local.json`（用户名/密码，600）+ `xingchen.playwright-state.json`（会话）。字段样例在技能 `config/xingchen.local.example.json`。也可环境变量 `XINGCHEN_USER` / `XINGCHEN_PASSWORD`。
- **不要**读「内网勿外传」口令 md，**不要**复用 `~/.cache/chrome-devtools-mcp/chrome-profile`。
- 现网探测：技能家 `方案与文档/20260901_账套与API现网.md`
- 科目余额期间 400 → 凭证 5 开头汇总；核算项目 OpenAPI 404 → 凭证部门辅助或引出。
- 空账残留服务 `7914379139221` 不是文化/上海/湖南，禁止当正式账。
- 更新财务skills 不得覆盖本机 `*.local.json` 和 `xingchen.playwright-state.json`。
- 薪酬台账（`config/薪酬台账.json`）：sheet 名先去掉数字再认，`equals` 精确、`contains` 包含；台账换月不用改配置。新出现的 sheet 认不出 → 在 `sheet_entity` 加一条。部门对不上 → 先看 `--profile` 的「对不上的部门」，再往 `org2_to_dept` / `center_to_dept` / `entity_dept_override` 加；禁止猜外地/大系统/长沙。
- 没有真台账时的排障路径：她机器上跑 `payroll_ledger.py --profile`（零金额）→ 认不出的 sheet/部门先补配置 → `--split-table` 出部门级拆分表 → 拆分表回传当 `--payroll-xlsx` 喂技能复现。原始台账（含姓名/证件号）不要回传。
