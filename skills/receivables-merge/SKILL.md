---
name: receivables-merge
description: >-
  合并每周应收账款台账：把按年份分 sheet 的「源台账」合并成一张主表，算账龄、从上一版「回填源」回填销售标注列、按「销售变化维护表」做销售归属、删掉已回款(应收=0)的行、再出透视汇总。
  当用户说「跑本周应收 / 应收合并 / 处理应收台账 / 把赵的应收表合一下 / 应收账款自动化 / 做应收all」，或给到一个含多年份 sheet 的应收 Excel 要合并清洗时，主动使用本技能。涉及"应收账款合并/账龄/回填/按销售拆分前的清洗"都应触发。
---

# 应收账款合并（赵 Excel 流程）

把财务部每周手工做的"应收账款合并"自动化。完整流程与口径依据见 `references/SOP_应收账款合并.md`（**动手前先读它**），设计思路见 `references/思路_skills化.md`。

## 你（agent）该怎么干

**核心原则：用它的是财务同事（如赵亮晶），不是技术员。** 她扔个 Excel 进来，你把活办完——找文件 / 认文件 / 跑脚本 / 出复核全归你；**别让她改文件名、摆文件夹、看命令、看 sheet 名**。

### ⭐ 人在环：什么自己干、什么问她（这是这个 skill 的灵魂，拿捏好分寸）

> 原则：**确定的、可逆的、常规的、她也答不上来的 → 自己干**；**只有她才知道的、有歧义的、对外发出去的产物 → 问她**。问要：**批量问（别一条条）、用人话（别甩告警/sheet）、给摘要（别甩原始数据）**。问太多=橡皮图章，她会烦、会乱点。

**自己干（别问，问了烦人）**
- 跑脚本、算账龄、删已回款(应收=0)行、排序、出透视——确定性、可复现的活。
- 认文件：能按内容认出源台账/回填源就**直接认**，**认对了只一句话告知**（"我把 X 当本周源表、Y 当上一版 all 了"），不用她逐个确认。
- 命令、列名映射、sheet 名、参数——你消化，绝不甩给她。

**必须问她（批量、人话）**
1. **跑之前，一句话问销售有没有变动**（她才知道，组织每周可能变）：
   *"跑之前确认下：这周销售人员有谁离职 / 新接手、或大客户换人负责吗？没有就回'没变'，我按现有维护表来。"*
   —— 只要一句、她能秒回；文件**有歧义**时顺带一起问哪个是源表/哪个是 all（不歧义就别问）。
2. **她的 Excel 和以前不一样**（你查出来的"认列告警"），**用人话问、别甩告警表**：
   - 列名变了 → *"你这周表里'应收金额'好像改叫'应收款'了，是同一个意思吗？"*
   - 多了个表 → *"多了个叫'9月批量'的 sheet，这是啥？年份算哪年、整批按哪个月算账龄？"*
3. **跑完，把"归属"和"要她拍板的"摘要给她看**（人话摘要，不是甩 sheet）：
   *"这次把高美杰的账分给了梁玲玲/于占国、骆利飞→王雄……（列关键几条）；另有 X 处拿不准（如疑似把'牛雪如'当'牛雪茹'）。对吗，要改吗？"*

### 1. 找齐三个输入（按内容认，不靠文件名）
需要：①**源台账**（本周原始，多个年份 sheet）②**回填源**（上一版做好的 all，带销售标注列；**还用于结转老坏账**）③**维护表**（`config/销售归属维护表.md`，两段：离职→接手 / 客户→接手；默认已在 `config/`）。

⚠️ **先 `cd` 到 skill 根目录再跑所有命令**（命令里有相对路径；不 cd 会找不到脚本/配置）。脚本自带的 `config/` 不受工作目录影响，但**别再手动传 `--rules`**——省略它就自动用 skill 自带的维护表，最稳。

```bash
cd "/Users/evanlee/Documents/甲骨易实习/2_战略专项/财务部Agent员工/skills/receivables-merge"
# 先识别看认得对不对（源台账/回填源按内容认）
python3 scripts/merge.py --inspect --input-dir <放文件的绝对目录>
```
- 默认去 `工作区/input/` 找；用户文件在别处就把 `--input-dir` 指过去（**用绝对路径**）。
- 认出来后：**不歧义就一句话告知**（"源台账=X、回填源=Y，我按这个跑了"）；**有歧义 / 认不准 / 缺文件才问她**——并趁这一句**顺带问"这周销售有没有变动"**（见上面人在环）。**别每个文件都要她点头。**

### 2. 跑合并（用绝对路径、别传 --rules）
```bash
python3 scripts/merge.py \
  --source <源台账绝对路径> --ref <回填源绝对路径> \
  --out 工作区/output/应收all_<日期>.xlsx
# 账龄基准月默认从源文件名取；补往月用 --base-month 202606
```
- **不要传 `--rules`** → 自动用 `config/销售归属维护表.md`（绝对解析、最稳）。万一你确实要指定，必须用**绝对路径**，否则相对路径在错误工作目录下会报错。
- `--ref`（上一版 all）省略则跳过回填**和老坏账结转**（标注列留空、缺老年份账）。**要完整复现成品，必须给 `--ref`。**
- 脚本按 SOP 跑 S1–S8：合并 → 账龄 → 回填 → **销售归属（维护表驱动）** → **结转老坏账** → **删应收=0行** → 复核 → 排序 → **透视汇总**。

### 3. ⭐ 确认销售归属（每次必做，但用"人话摘要"，不甩 sheet）
**销售变更是「会变、要人确认」的事，绝不能由脚本/你私自拍板。** 跑完后：
1. **你自己**看产物里的 `归属变更`（谁→归给谁、各多少行）和 `归属存疑`（疑似错别字/对不齐的名，如「牛雪如 vs 牛雪茹」）两个 sheet——**这是给你看的，不是给她看的**，把关键的提炼成人话。
2. **用人话摘要问她**（别甩 sheet）："这次把高美杰的账分给了梁玲玲/于占国、骆利飞→王雄……（列关键几条）；有 X 处拿不准（列出）。**对吗，要改吗？**"
3. **她说"确认"** → 定稿往下走。
4. **她说要改**（如"骆利飞归王雄""牛雪如就是牛雪茹"）→ 按她说的**改 `config/销售归属维护表.md`**（加人/改名/调接手）→ **重跑** → 再确认。
5. **改完把改动固化**：`git add config/销售归属维护表.md && git commit`，让这次的销售变更留痕、下次自动生效。

> 维护表是"活"的：每周可能有人离职/客户转手。**你的职责是把变更和存疑摆清楚、让人一句话拍板，然后忠实落到表里**——不是自己猜规则、不是写死在代码里。

### 4. ⭐ 认列告警 → 用人话问她（表变了的早期信号）
**跑完你先看 `认列告警` sheet（给你看的，运行日志也会打 ⚠）**——这是她表变了的关键信号：
- **「关键列没认出」**：她表里某列改了名（如"应收金额"→"应收款"），这批会**丢失/算错**。→ **用人话问她**（见人在环②）确认是不是同一个意思，确认后把新列名加进 `config/列名别名.json`，重跑。
- **「未知 sheet」**：多了个不是年份、没登记的 sheet（如"9月批量"）。→ **别猜**，人话问她这是不是新批次、记哪年/哪月，确认后补进 `config/业务规则.md`，重跑。

其余产物（`未匹配复核(#N/A)`、`离职残留`应为空、`被删0行`留底、`透视汇总`、`运行报告`）**你自己核**；只有"被删的是不是真已回款""#N/A 那几单要不要补数据"这类**她才知道的**才提她一句。别把一堆 sheet 全甩给她。

## 会变的东西在哪（都是活的，改表不改码）
- **列名别名**（认列规则）→ `config/列名别名.json`
- **销售归属**（离职→接手、客户→重分配）→ `config/销售归属维护表.md`（用户维护、每次确认）
- **特殊批次 / 跳过的 sheet / 业务规则说明** → `config/业务规则.md`（agent 跑前先读；遇表里没覆盖的情况停下问人）
确定性算术（账龄、合并、删0、结转、求和）写死在脚本里保证可复现；**会变/要判断的都在上面这几张 MD/表里**。

## 验收口径（判断脚本对不对）
**已知基准**（源台账 `2026.6.4日应收` + 回填源 `2026.5.28应收all`，`--base-month 202606`）：
- 主表 **≈3247 行**；高美杰三路 **梁玲玲-高美杰 104 / 于占国-高美杰 74 / 高美杰1 10**；删0行 **175**；结转老坏账 **29**；离职残留 **0**。
- ⚠ **3247 vs 成品 3243 的 +4 行是正常的、已知的**：成品的 3243 行**一行不缺都在**（成品有·我无=0），多出的 4 行是 ¥0.61/¥1 这类极小额（成品删了、脚本保留）+ 结转边界，不是错。**别因为对不上 3243 就以为跑错了。**
- 还剩 6 行销售归属与成品不同（特变/广州库洛）= 成品自身不一致，等亮晶姐定，见 `20260618_应收归属待确认_问亮晶姐.docx`。

换别的周数据跑：看 `认列告警`（应为空）、`离职残留`（应为空）、年度分布合理、不崩，即结构稳。
