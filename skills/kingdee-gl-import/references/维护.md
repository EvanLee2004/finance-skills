# 序时账入金蝶 · 维护

- 金蝶换官方引入模板：用当前账套「凭证引入」下载空模，替换 `config/凭证引入空模.xlsx`。必须保留 `sheet1（名称勿改）` 和隐藏对照 sheet。
- 7 期教训：附件列有数会把后面分录拆成新凭证。脚本写死清空，不要加「保留附件」开关。
- 湖南分科目族是 5602xx/5603xx，不是总部 55xxxx。本技能按源表编码原样抄，不改科目。
- 不要并进 `kingdee-posting`。那三个模块要总部档案 API。
